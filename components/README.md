# Components

Issue #4 defines the component system; Issue #5 implements it. Candidate names include TavernNotice, DumpsterDivider, CharacterCard, WantedPoster, MerchCard, TankardButton, TavernQuote, IncidentForm, SkitCard, and FlamingDumpsterBadge.
